extends layout

block content
    section.desk
        h1= `${error.message} - Status: ${error.status}`
        img.desk-items(src="/img/desk.svg", alt="Desk")
        a.button-primary-small(href="/") Go Home
        .paper
            pre.stack-trace Stack Trace would be here
        .tear