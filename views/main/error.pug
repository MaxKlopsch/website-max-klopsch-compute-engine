extends layout

block content
    main
        h1= `An Error Has Occured: ${error.status} - ${error.message}`
        section
            if error.stack
                pre #{error.stack}
            p.center Check that you typed the address correctly, go back to your previous page or 
                a(href="/") Go Home
                | .